/**
 * WorkAxle Sync — Extension Configuration Template
 *
 * Copy this file to config.js and fill in your own values.
 *
 * Steps:
 *   1. Deploy cloudflare-worker.js to your own Cloudflare Worker (free)
 *   2. Add OWNER_SECRET and WORKER_BASE_URL as secrets in your Worker
 *   3. Set the values below to match
 */

const WORKAXLE_CONFIG = {
  WORKER_BASE_URL: 'https://your-worker.your-subdomain.workers.dev',
  OWNER_SECRET: 'your-secret-here'
};
